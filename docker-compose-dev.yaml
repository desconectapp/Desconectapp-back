services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: hot-reloading-gin
    ports:
      - "8080:8080" 
    volumes:
      - ./src:/app
    environment:
      PORT: "8080"

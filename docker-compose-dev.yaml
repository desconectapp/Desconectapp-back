services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: hot-reloading-gin
    ports:
      - "${APP_PORT:-8080}:${APP_PORT:-8080}" 
    volumes:
      - ./src:/app
    environment:
      PORT: "8085"
      GIN_MODE: debug
    env_file:
      - .env

  postgres:
    image: postgres:16.4-alpine3.20
    ports:
      - 5433:5432
    env_file:
      - .env
    volumes:
      - ./database/postgres:/var/lib/postgresql/data
      - ./src/db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./src/db/files/activities.csv:/activities.csv
      - ./src/db/files/users.csv:/users.csv
      - ./src/db/files/activity_requests.csv:/activity_requests.csv
      - ./src/db/files/users_preference.csv:/users_preference.csv
      - ./src/db/files/profiles.csv:/profiles.csv
      - ./src/db/files/groups.csv:/groups.csv
      - ./src/db/files/group_members.csv:/group_members.csv
      - ./src/db/files/communities.csv:/communities.csv
      - ./src/db/files/communities_members.csv:/communities_members.csv

  pgadmin:
    image: dpage/pgadmin4:7.5
    container_name: desconectapp_pg_admin
    restart: always
    env_file:
      - .env
    ports:
      - "5050:80"
    volumes:
      - pgadmindata:/var/lib/pgadmin

volumes:
  pgadmindata:
